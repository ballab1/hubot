# build Packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    declare PKGS='file gnutls-utils nodejs-dev build-base g++ gcc-objc libtool libc6-compat make expat-dev gnupg tar git'
    package.checkIfInstalled "$PKGS"
    apk add --no-cache --virtual .buildDepedencies $PKGS
fi

